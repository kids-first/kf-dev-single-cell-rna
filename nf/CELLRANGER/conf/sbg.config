process {
    withName: COUNT {
        afterScript = { "rm -rf fastqs ${reads.join(' ')} ${mates.join(' ')} ${indices ? indicies.join('') : ""} $transcriptome" }
    }
    withName: MULTI {
        afterScript = { "rm -rf FASTQS $sample_sheet $probe_set_csv $transcriptome" }
    }
}